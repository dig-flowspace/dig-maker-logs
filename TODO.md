# TODO

> Exactly **one** ▶ Active now. Everything else is shelved (safe, not lost) or
> in the 📥 Inbox (seen, not yet registered). Promoting and shelving is Dan's
> call.

## ▶ Active now

- ▶ Decide the first real job: pick up the Ender 3 CNC build, the camera
  slider, or the Reddit tooling — and tell Kilgore, so it lands here properly.

## On the shelf

- Ender 3 CNC conversion — build against the `EnderCNCs/` reference.
- Camera slider — build against the `DIY3AxisCameraSlider/` reference.
- Reddit tooling — fill `.env` from `.env.example` when it starts.

## 📥 Inbox

*(empty — new ideas land here first)*
